● Epic #30 itself doesn't — it's a coordination tracker; the design work lives in the children. For the sub-issues:

- #33 (composition.md rewrite) — /define only. Research is done (wiki), target file known, scope bounded.
- #34 (orchestrator audit) — /discovery → /define. Genuine investigation: we don't yet know which spawn sites downgrade vs defend, and there are 4 skills to survey.
- #35 (specialist audit) — same as #34, /discovery → /define.
- #36 (authoring templates) — /define only. Known files, mechanical edits.
- #37 (measurement) — /define only. Methodology question (pick flow, define quality check), no investigation phase needed.

Main tradeoff: #34/#35 could be collapsed to /define-with-investigation-pass if you want to move faster, but the two audits have enough unknowns that /discovery probably earns its keep. Want me to open the /discovery and /define tasks
now, or wait until #33 is merged first so the rubric they audit against actually exists?
